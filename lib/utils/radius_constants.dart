import 'package:flutter/material.dart';

const Radius radiusSmall = Radius.circular(8.0);
const Radius radiusMedium = Radius.circular(18.0);
const Radius radiusLarge = Radius.circular(24.0);

const BorderRadius borderRadiusSmall = BorderRadius.all(radiusSmall);
const BorderRadius borderRadiusMedium = BorderRadius.all(radiusMedium);
const BorderRadius borderRadiusLarge = BorderRadius.all(radiusLarge);

const BorderRadius borderTopRadiusSmall = BorderRadius.vertical(top: radiusSmall);
const BorderRadius borderTopRadiusMedium = BorderRadius.vertical(top: radiusMedium);
const BorderRadius borderTopRadiusLarge = BorderRadius.vertical(top: radiusLarge);

const BorderRadius borderBottomRadiusSmall = BorderRadius.vertical(bottom: radiusSmall);
const BorderRadius borderBottomRadiusMedium = BorderRadius.vertical(bottom: radiusMedium);
const BorderRadius borderBottomRadiusLarge = BorderRadius.vertical(bottom: radiusLarge);

const BorderRadius borderRightRadiusSmall = BorderRadius.horizontal(right: radiusSmall);
const BorderRadius borderRightRadiusMedium = BorderRadius.horizontal(right: radiusMedium);
const BorderRadius borderRightRadiusLarge = BorderRadius.horizontal(right: radiusLarge);

const BorderRadius borderLeftRadiusSmall = BorderRadius.horizontal(left: radiusSmall);
const BorderRadius borderLeftRadiusMedium = BorderRadius.horizontal(left: radiusMedium);
const BorderRadius borderLeftRadiusLarge = BorderRadius.horizontal(left: radiusLarge);
