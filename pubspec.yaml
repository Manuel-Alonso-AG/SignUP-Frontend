name: frontend
description: "Sign Up"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl_phone_number_input: 0.7.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

fonts: 
  - family: BAHNSCHRIFT
    fonts:
      - asset: fonts/BAHNSCHRIFT.TTF